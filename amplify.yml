version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install -g pnpm@latest-10
    build:
      commands:
        - ls -la
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
